# LinkedIn Connection Checker Environment Configuration
# Copy this file to .env and fill in your values

# ======== Stagehand Configuration ========
# You can leave this as is for local testing
BROWSERBASE_PROJECT_ID=c9488675-4c8f-468a-b113-ffaf27782bab
# BROWSERBASE_API_KEY=your_browserbase_api_key (Optional)

# ======== LinkedIn Credentials ========
# No need to add credentials - you'll log in manually in the browser window
# The app will save cookies to avoid repeated logins

# ======== Google Sheets Configuration ========
# Add your Google Spreadsheet ID below (from your sheet's URL)
# Example: https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit
SPREADSHEET_ID=your_spreadsheet_id

# The name of your sheet (default is Sheet1)
SHEET_NAME=Sheet1

# Your sheet should have LinkedIn profile URLs in column A starting from row 2 (A2)
# Column B will be updated with the connection status

# ======== Google API Configuration ========
# Get a Google API key from https://console.cloud.google.com/apis/credentials
# Enable the Google Sheets API in your Google Cloud Console project first
GOOGLE_API_KEY=your_google_api_key

# Important: For the API key to work, either:
# 1. Change your Google Sheet sharing settings to "Anyone with the link can edit" OR
# 2. Set up OAuth (see below)

# ======== OAuth Setup (Alternative to API Key) ========
# For more secure access, you can use OAuth instead
# GOOGLE_CLIENT_ID=your_google_client_id
# GOOGLE_CLIENT_SECRET=your_google_client_secret
# GOOGLE_REDIRECT_URI=http://localhost:3000/oauth2callback

# ======== AI Configuration ========
# Choose which AI provider to use: "anthropic" or "openai"
AI_PROVIDER=anthropic

# Anthropic API Key (Claude)
# Get your API key from https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your_anthropic_api_key

# OpenAI API Key
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key

# ======== Advanced Settings ========
# Delay between profile checks in milliseconds (3000 = 3 seconds)
# PROFILE_CHECK_DELAY=3000

# Browser settings - set to true for headless mode (no browser UI)
# HEADLESS_BROWSER=false 